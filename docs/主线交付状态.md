# 原创主题包主线交付状态

- 共享主题源：5/5；
- Codex 单目标主题包：5/5；
- WorkBuddy 单目标主题包：5/5；
- 静态主题校验：10/10 PASS；
- Node.js 回归测试：6/6 PASS；
- Codex 实机验证：NOT_RUN；
- WorkBuddy 实机验证：NOT_RUN；
- restore 实机验证：NOT_RUN。

本文件用于记录进入主线前的交付边界。真实客户端状态只有在对应应用版本上完成 `theme inspect`、`probe`、`apply`、`verify --screenshot` 与 `restore` 后才能升级。
